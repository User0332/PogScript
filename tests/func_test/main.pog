
# argv can't be marked as char ptr ptr because the parser
# and compiler don't currently support pointer stacking
int func main(int var argc, char ptr argv)
{
	int var foo = 45
	int var bar = foo+86
	int var baz = (foo+bar)*(86-foo)
}